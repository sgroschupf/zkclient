<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.out</target>
        <encoder>
            <!--
            This pattern was converted from Log4j pattern.
            Note that logging line number (%L) is slow and not recommended!
            -->
            <pattern>%d{yy/MM/dd HH:mm:ss} %5p [%thread] %logger:%L - %msg%n</pattern>
        </encoder>
    </appender>
    <logger name="org.apache.zookeeper" level="error"/>
    <root level="info">
        <appender-ref ref="console"/>
    </root>
</configuration>